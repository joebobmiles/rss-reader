services:
    backend:
        build:
            dockerfile: docker/backend/dev/Dockerfile
        restart: "no"
        ports:
            - "80:33287"
        volumes:
            - .:/srv/hullabaloo